# Release Notes

## v1.0.0
- public repository launch
- installer and bootstrap flow
- role-separated multi-agent operating model
- starter prompts and eval seeds
- richer bootstrap scaffold documents
- lane-specific task instruction samples
- seeded example workspace and operator playbook
- social preview asset and setup guide

## Packaging notes
- release tarball is produced by `./package-release.sh v1.0.0`
- quick installer expects `SKILL_REPO_URL` to point at the hosted `.tar.gz` asset
